BASE URl == https://handy-service-server.herokuapp.com/

/user/login    			method POST   body email, password
/send/otp      			method POST   body number
/verify/otp    			method POST   body id, otp
/user/register  		method POST   body {name, email, number, pass}
/user/forgot-password   method POST   body email
/user/reset-password    method POST   body email, password

/services/all          	method  GET   query city       									return service list
/reviews				method GET    query service                 					return reviews list
/request/callback		method POST   body {user_id, name, email, mobile, location}
/review           		method POST   body {cus_id, service_id, rating, message}
/service/reviews        method GET      query service_id                                    return reviews list
/search/services		method GET    query  {city, name, type}							return service list
/book/service           method POST   body {user_id, service_id, scheduled_date, scheduled_time, reason, address}
/feedback               method POST   body {type, name, email, mob, message}
/locations/enabled      method POST   body empty                                        return list of enabled cities
/image                  method POST   body {the image file}   query {id: user_id}       return  the url of the uplaoded image from s3.

NOT DONE YET:::::::::::
/create/corporate       method POST   body {name, email, mobile, org_name, message, location}
